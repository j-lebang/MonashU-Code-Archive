import { TransferTimePipe } from './transfer-time.pipe';

describe('TransferTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TransferTimePipe();
    expect(pipe).toBeTruthy();
  });
});
